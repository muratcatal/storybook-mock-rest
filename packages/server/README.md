This will run a localserver
localhost:8081
/endpoints
    GET: Will list mocked endpoints
    POST: Will save a new endpoint
    PUT: will update endpoint
/test
    will serve a page to test mocked endpoint